[project]
name = "grpo-test"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "tensorboard>=2.21.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "trl>=1.9.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
